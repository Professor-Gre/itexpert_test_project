import { createStore } from 'vuex'
import data from '@/store/modules/data'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    data
  }
})
